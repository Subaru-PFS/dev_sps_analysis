setupRequired(pfs_pipe2d)
#setupRequired(pfs_utils)
setupRequired(spt_operational_database)

pathPrepend(PATH, ${PRODUCT_DIR}/bin)
pathPrepend(PATH, ${PRODUCT_DIR}/scripts)
pathPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
